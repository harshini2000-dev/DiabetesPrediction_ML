# DiabetesPrediction_ML
Contains code of web app to predict diabetes using diagnostic factors (Glucose, BMI, Age, Insulin, Blood Pressure, etc.) which yields approx 96.6% accuracy, leveraging the PIMA dataset, implemented (Support Vector Machine) SVM  and Random Forest classifier algorithm. 
