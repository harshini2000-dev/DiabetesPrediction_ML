# DiabetesPrediction_ML
Contains code of web app to predict diabetes using diagnostic factors (Glucose, BMI, Age, Insulin, Blood Pressure) which yields 89.9% accuracy, leveraging the PIMA dataset and (Support Vector Machine) SVM algorithm. 
